@model IEnumerable<ComplianceAuditWeb.Models.ListOfGroupCompanies>
@{
    ViewBag.Title = "Group Company Dashboard";
}
<ul class="breadcrumb ML15">
    <li><a href="/Common/dashboard?pid=6">Group Company Management</a></li>
    <li>Group Company Dashboard</li>
</ul>
<h2 class="ML15">Group Company Dashboard</h2>

<div class="new_section_box">
    <div class="tab-content">
        <div id="home" class="tab-pane fade in active">
            <h2 class="big_hdng" style="text-transform:none; border:0; margin-top:15px;"></h2>

            @if (TempData["Success"] != null)
            {
                <center>
                    @*<p class="alert alert-success"  style="width:1000px">  @TempData["Company"]</p>*@
                    <p id=" successMessage"> @TempData["Company"] @TempData["Success"] </p>
                </center>
            }
            @foreach (var item in Model)
            {
                <div class="col-sm-3 QueList">
                    <div class="queContainer">
                        @*@item.OrganizationID = @Session["BranchID"];*@
                        <a href="@Url.Action("AboutGroupCompany", "ManageOrganization",new { id = item.OrganizationID })">
                            @if (item.IsActive == false)
                            {
                                <i class="fa fa-ban"></i>
                            }
                            else
                            {
                                <i class="fa fa-check-circle"></i>
                            }
                            <span class="qIcon"><img src="/UploadedFiles/@item.Logo" style="height:50px;width:85px" /></span>
                            <h3 class="PB25"> @Html.DisplayFor(modelItem => item.CompanyName)</h3>
                        </a>
                        <div class="clearfix"></div>
                    </div>
                </div>
            }

            <div class="clearfix"></div>

        </div>

    </div>

</div>

